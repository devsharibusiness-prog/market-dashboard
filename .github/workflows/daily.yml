name: Daily Market Fetch

on:
  schedule:
    # 13:00 UTC daily, Mon–Fri = 6:30 PM IST = ~9:00 AM ET (standard time).
    # Cron is ALWAYS in UTC.
    - cron: '30 12 * * 1-5'
  workflow_dispatch:   # lets you trigger it manually from the Actions tab

jobs:
  fetch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: python fetch_data.py
        env:
          FINNHUB_KEY: ${{ secrets.FINNHUB_KEY }}
          NEWSAPI_KEY: ${{ secrets.NEWSAPI_KEY }}
      - run: python suggest.py
      - name: Commit refreshed data.json
        run: |
          git config user.name "market-bot"
          git config user.email "bot@users.noreply.github.com"
          git add data.json suggestions.json market.json
          git commit -m "Daily data refresh $(date -u)" || echo "no changes"
          git push
